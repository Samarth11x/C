#include<stdio.h>
// print 1(true) or 0(false) for the following statement :
//  if it's sunday & it's raining -> (true)1

int main(){
    int if_sunday = 1;
    int if_snowing = 1;
    printf("%d\n", if_sunday && if_snowing );
    
    return 0;
}